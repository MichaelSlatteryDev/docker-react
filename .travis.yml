language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t michaelslatterydev/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true michaelslatterydev/docker-react npm run test
